CREATE DATABASE eletronicos_store;
USE eletronicos_store;

CREATE TABLE usuarios(
IDUSUARIO INT PRIMARY KEY AUTO_INCREMENT,
NOME VARCHAR(255) NOT NULL,
SOBRENOME VARCHAR(255) NOT NULL,
EMAIL VARCHAR(50) UNIQUE NOT NULL,
SENHA VARCHAR(50) NOT NULL ,
STATUS TINYINT(1) DEFAULT 1
);

CREATE TABLE grupo_usuario(
IDGRUPO INT PRIMARY KEY AUTO_INCREMENT,
NOME ENUM ('ADMINISTRADOR', 'ESTOQUISTA')
);

ALTER TABLE usuarios
ADD ID_GRUPO_USUARIO INT NOT NULL;

	ALTER TABLE usuarios
	ADD FOREIGN KEY ID_GRUPO_USUARIO (ID_GRUPO_USUARIO)
	REFERENCES grupo_usuario (IDGRUPO);